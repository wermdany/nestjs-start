{
  "extends": "./tsconfig.json",
  // ⚠️ `exclude` **不会**与基类合并（是覆盖），所以 `packages` 必须在这里再写一次：
  // 拉进契约包源码会让 rootDir 从 `src` 变成仓库根，产物变成 `dist/src/main.js`。
  "exclude": ["node_modules", "dist", "test", "packages", "**/*spec.ts"]
}
